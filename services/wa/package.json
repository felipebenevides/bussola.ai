{
  "name": "bussola-wa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bússola — Evolution API v2 adapter (webhook in / send out / instance lifecycle). Runs on Bun + Railway.",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "hono": "^4.6.14",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
